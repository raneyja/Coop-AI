---
description: Marketing site canonical source — coop-ai.dev / main branch / HeroDemoArtifact
globs: website/**/*
alwaysApply: false
---

# Marketing site — canonical source

**Production truth:** [https://coop-ai.dev](https://coop-ai.dev) (deployed from `main`).

When editing the homepage, hero, or marketing demos, use the **current** stack — not legacy dark-theme VS Code mock carousels.

## Current homepage (local mock)

Dark environment with **deployed page order**: hero → the gap → logos → quotes → close. Inner pages stay light.

| Piece | File |
|-------|------|
| Page theme | `website/src/components/HomePageTheme.tsx` (`body.home-page`) |
| Hero layout | `website/src/components/Hero.tsx` → galaxy bg + `<HeroDemoArtifact tone="dark" />` |
| Hero space | `website/src/components/HeroGalaxyBackground.tsx` |
| The gap | `website/src/components/HomeStackContextSection.tsx` |
| Quotes | `website/src/components/Testimonial.tsx` |
| Demo artefacts | `website/src/components/HeroDemoArtifact.tsx` |
| Home close | `website/src/components/HomeCloseSection.tsx` (no zsh terminal) |
| Site copy | `website/src/lib/site.config.ts` (`tagline`, `subheadline`) |
| Light inner pages | `website/src/components/SiteAtmosphere.tsx` + `layout.tsx` (paper, grid, wash) |

Ask / Change / Indexed live on `/product`, not the homepage.

Demo flow labels: `// question` → `// pulling context from` → `// context found` → `// response` / `// edit` / `// complete`. Scenario dots on the artifact must stay clickable.

## Legacy — do NOT treat as production homepage

| Legacy | File | Notes |
|--------|------|-------|
| Dark VS Code mock carousel | `website/src/components/FileContextStoryDemo.tsx` | `/file-context-demo` preview only |
| Old hero mock | `website/src/components/HeroProductMock.tsx` | Unused on production hero |
| Product page carousel | `website/src/components/ProductShowcaseCarousel.tsx` | `/product` only |
| Fake terminal CTA | `website/src/components/CTASection.tsx` | Inner pages only (`/product`, `/enterprise`, …) |

Before changing hero demo behavior, **confirm `Hero.tsx` imports `HeroDemoArtifact`**, not `FileContextStoryDemo`.

## Branch drift

Feature branches may lag `main` on `website/` (missing `HeroDemoArtifact.tsx`, old `Hero.tsx`). If local preview looks wrong:

1. Compare: `git diff main -- website/src/components/Hero.tsx`
2. Restore from main: `git checkout main -- website/` (or merge `main` into your branch)
3. Restart dev server after checkout

## Local preview

| Step | Detail |
|------|--------|
| Port | **3001** — `website/package.json` → `"dev": "next dev -p 3001"` |
| URL | `http://localhost:3001` (not 3000) |
| Command | `cd website && npm run dev` |
| Success | Dark home, galaxy hero, **the gap** then logos then quotes. Inner pages stay **light paper** (grid + wash), not flat white. Ask / Change / Indexed on `/product`. |

Never point users at `localhost:3000` for the marketing site unless they explicitly started Next on that port.
