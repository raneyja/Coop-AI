import React from "react";

/** Shared divider + label before collapsible evidence sections on every Sources card. */
export function EvidenceSectionsIntro(): React.ReactElement {
  return <p className="coop-result-evidence-intro">Source details</p>;
}
