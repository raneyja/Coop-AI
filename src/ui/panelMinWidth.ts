/** Minimum comfortable width for the CoopAI sidebar webview (px). */
export const COOP_PANEL_MIN_WIDTH = 320;

/** Approximate pixels added per `workbench.action.increaseViewWidth` step. */
export const SIDEBAR_WIDTH_STEP_PX = 8;

export const SIDEBAR_WIDTH_ENFORCE_MAX_STEPS = 25;

export const SIDEBAR_WIDTH_ENFORCE_COOLDOWN_MS = 150;
