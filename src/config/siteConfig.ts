/** Canonical marketing site — not the API host (see DEFAULT_API_BASE in chat/types.ts). */
export const MARKETING_SITE_URL = "https://coop-ai.dev";

export const PRICING_PAGE_URL = `${MARKETING_SITE_URL}/pricing`;

export const MARKETING_DOMAIN = "coop-ai.dev";

export const CONTACT_EMAIL = "hello@coop-ai.dev";
